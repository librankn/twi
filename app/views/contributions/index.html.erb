<h1>ツイート一覧</h1>

<p><%= notice %></p>

<table>
  <tr>
    <th>内容</th>
  </tr>

<% @contributions.each do |contribution| %>
  <tr>
    <td><%= contribution.content %></td>
    <td><%= link_to "ツイートを編集する",edit_contribution_path(contribution.id) %>
    <td><%= link_to "ツイートを削除する",contribution_path(contribution.id),method: :delete, data: { confirm: '本当に削除していいですか？' } %></td>
  </tr>
<% end %>
</table>
<br>
<%=link_to '新しくツイートを投稿する',new_contribution_path%>
